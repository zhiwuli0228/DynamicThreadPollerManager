# v0.14.0 SR 功能设计

## Header

- Document type: SR functional design
- Version name: `v0.14.0`
- Status: `DRAFT`
- Current phase: `SR_DRAFT`
- Authoring date: `2026-06-14`
- Input baseline: `00-objectives-and-scope.md`, `decision-log.md`, `10-ir.md` (post-disposition, IR closure verified), `11-ir-review.md`, `12-ir-review-disposition.md`, `13-ir-closure-verification.md`
- IR FIX items incorporated: F01 (PolicyEvaluationInput path), F02 (SafetyGate 3-arg + ReadinessAssessment), F03 (calibrate returns new scorer), F04 (Outcome enum), F05 (remove cooldownPeriodMs, delegate to SafetyGate), F06 (EvidenceRecorder snapshot path), F07 (remove redundant scorer), F08 (reset method), F09 (runId=sessionId), F12 (concurrency risk P1)
- Current conclusion: functional design complete; not an OpenSpec change and not an implementation authorization

## 1. 设计目标

v0.14.0 SR 将已闭环 IR 转成后续 OpenSpec change 可执行的功能设计。目标：在 v0.13.0 诊断层（PressureClassifier, PolicyScorer, PolicyRanker）、v0.7.0-v0.10.0 执行层（AdjustmentAdapter, SafetyGate, ScaleAdjustmentCommand）、v0.11.0 采样层（LivePressureSampler, EvidenceRecorder）之上，构建闭环编排层——`AdjustmentLoop` 作为顶层控制器，`DecisionOrchestrator` 编排诊断→决策链路，`OscillationDetector` 和 `AdjustmentHistory` 提供安全保障，`PressureStateMachine` 追踪状态转换，`FeedbackCalibrator` 基于实际结果校准评分权重。

关键架构约束：所有新组件位于新包 `experiment.loop`。不引入新的 executor mutation。不修改 v0.13.0 及之前版本的既有接口。`ThresholdPolicyScorer` 添加包级可见权重 getter（最小侵入）。

本设计只描述功能边界和组件契约，不授权 Java 实现，不授权 OpenSpec change 创建。

## 2. 设计结论

| 组件 | 类型 | 包 | 关键职责 |
|---|---|---|---|
| `LoopState` | enum | `experiment.loop` | 闭环生命周期 5 状态 + 合法转换 |
| `LoopConfig` | record | `experiment.loop` | 闭环配置（8 字段，不含 cooldown） |
| `LoopSession` | record | `experiment.loop` | 一次闭环会话的完整信息 |
| `AdjustmentDecision` | record | `experiment.loop` | 单次迭代的决策输出 |
| `TransitionLegality` | enum | `experiment.loop` | LEGAL / ANOMALOUS / ILLEGAL |
| `PressureStateTransition` | record | `experiment.loop` | 一次压力状态转换 |
| `PressureStateMachine` | class | `experiment.loop` | 合法转换定义 + 转换历史 + 当前状态 |
| `AdjustmentHistory` | class | `experiment.loop` | 调整记录存储 + 查询 + 成功计数 |
| `HistoryEntry` | record | `experiment.loop` | 单次调整记录（决策+结果+前后分类） |
| `OscillationDetector` | class | `experiment.loop` | 滑动窗口振荡模式检测 |
| `DecisionOrchestrator` | class | `experiment.loop` | 编排 classification→ranking→evaluation |
| `AdjustmentLoop` | class | `experiment.loop` | 闭环主控制器（生命周期+主循环） |
| `FeedbackCalibrator` | class | `experiment.loop` | 基于反馈的权重校准 |
| `LoopEvidenceRecorder` | interface | `experiment.loop` | 闭环迭代证据记录 |
| `LoopIterationEvidence` | record | `experiment.loop` | 单次迭代证据 |
| `ThresholdPolicyScorer` | 修改 | `experiment.classification` | 添加包级可见权重 getter |

## 3. 模块边界

| 模块 | 变更类型 | 职责 | 禁止事项 |
|---|---|---|---|
| `experiment.loop` | **新增** `LoopState` | 闭环生命周期枚举 | — |
| `experiment.loop` | **新增** `LoopConfig` | 闭环配置 record | — |
| `experiment.loop` | **新增** `LoopSession` | 闭环会话 record | — |
| `experiment.loop` | **新增** `AdjustmentDecision` | 调整决策 record | 不依赖 executor 包 |
| `experiment.loop` | **新增** `TransitionLegality` | 转换合法性枚举 | — |
| `experiment.loop` | **新增** `PressureStateTransition` | 状态转换 record | — |
| `experiment.loop` | **新增** `PressureStateMachine` | 状态机：合法转换表 + 历史追踪 | 不修改 PressureClassifier |
| `experiment.loop` | **新增** `AdjustmentHistory` + `HistoryEntry` | 调整历史存储 + 查询 | 不依赖 classification 包（仅存储引用） |
| `experiment.loop` | **新增** `OscillationDetector` | 振荡检测：乒乓/过度/策略切换 | 无状态（pure function） |
| `experiment.loop` | **新增** `DecisionOrchestrator` | 决策编排器 | 不修改 PolicyEvaluator/PolicyRanker 接口 |
| `experiment.loop` | **新增** `AdjustmentLoop` | 闭环主控制器 | 不直接操作 executor（通过 adapter） |
| `experiment.loop` | **新增** `FeedbackCalibrator` | 权重校准器 | 不修改 ThresholdPolicyScorer（返回新实例） |
| `experiment.loop` | **新增** `LoopEvidenceRecorder` | 证据记录接口 | — |
| `experiment.loop` | **新增** `LoopIterationEvidence` | 迭代证据 record | — |
| `experiment.classification` | **修改** `ThresholdPolicyScorer` | 添加包级可见权重 getter（4 个方法） | 不修改现有 public API |
| 所有其他包 | 不变 | — | — |

### 依赖方向

```text
experiment.loop (AdjustmentLoop, DecisionOrchestrator, PressureStateMachine,
                 OscillationDetector, AdjustmentHistory, FeedbackCalibrator,
                 LoopSession, LoopConfig, LoopState, AdjustmentDecision,
                 LoopEvidenceRecorder, LoopIterationEvidence)
    ├── experiment.classification (PressureClassifier, PolicyScorer, PolicyRanker,
    │                              PolicyScore, PressureClassification, PressureState,
    │                              ThresholdPolicyScorer, NormalizedPressureMetrics,
    │                              ClassifierConfig — 只读)
    ├── experiment.policy (PolicyEvaluator, ThresholdPolicyConfig, PolicyDecision,
    │                       PolicyEvaluationInput, PolicyAction — 只读/调用)
    ├── experiment.adjustment (ExecutorAdjustmentAdapter, RuntimeAdjustmentSafetyGate,
    │                           SafetyGateDecision, ScaleAdjustmentCommand,
    │                           AdjustmentResult, AdjustmentStatus, ExecutorStateSnapshot — 调用)
    ├── experiment.metrics (EvidenceRecorder, ObservedSnapshot, PressureSnapshot — 只读)
    ├── experiment.analysis (ReadinessAssessment, ReadinessStatus — 构造运行时实例)
    ├── experiment.executor (ManagedExecutor — 只读 getRejectedTaskCount/getCurrentPoolSize)
    └── java.time (Instant)
```

无循环依赖：loop 向下依赖已有包，已有包对 loop 无反向依赖。loop 包内部：AdjustmentLoop → DecisionOrchestrator → (PressureStateMachine, OscillationDetector → AdjustmentHistory)。

### 关键架构裁决

**Why new package `experiment.loop`**: 闭环编排是一个新的控制层，与已有的 11 个包正交。它消费诊断（classification）、决策（policy）、执行（adjustment）三个层的接口，但不修改它们。独立包避免在现有包之间引入控制依赖。

**Why LOOP cooldown delegates to SafetyGate**: SafetyGate 已有成熟的 cooldown 机制（`cooldownRemaining` 计数器 + `recordApplied()` 重置）。闭环不应重复实现。等效 wall-clock 冷却 = `SafetyGateConfig.cooldownDecisionIntervals() × samplingIntervalMs`。

**Why FeedbackCalibrator returns new scorer**: `ThresholdPolicyScorer` 是不可变的（`private final` 权重字段）。遵循不可变模式——calibrator 读取当前权重（通过新 getter），计算新权重，返回新实例。AdjustmentLoop 替换 DecisionOrchestrator 中的 scorer 引用。

**Why PressureStateMachine is separate from SnapshotPressureClassifier**: 分类器保持无状态（每次 `classify()` 独立）。状态机追踪跨调用的转换历史。两者职责正交。

## 4. 组件设计

### 4.1 LoopState

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.loop`

**职责**: 闭环生命周期状态枚举。

```java
public enum LoopState {
    IDLE,
    RUNNING,
    PAUSED,
    STOPPED,
    EMERGENCY_STOPPED
}
```

**合法转换表**（实现在 `AdjustmentLoop` 中验证）：

| 从 | 到 | 合法 |
|---|---|---|
| IDLE | RUNNING | ✓ start() |
| RUNNING | PAUSED | ✓ pause() |
| PAUSED | RUNNING | ✓ resume() |
| RUNNING | STOPPED | ✓ stop() |
| PAUSED | STOPPED | ✓ stop() |
| RUNNING | EMERGENCY_STOPPED | ✓ emergencyStop() |
| PAUSED | EMERGENCY_STOPPED | ✓ emergencyStop() |
| EMERGENCY_STOPPED | IDLE | ✓ reset() |
| STOPPED | IDLE | ✓ reset() |
| 所有其他组合 | — | ✗ IllegalStateException |

---

### 4.2 LoopConfig

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.loop`

**职责**: 闭环配置参数。不含 cooldownPeriodMs（委托给 SafetyGate）。

```java
public record LoopConfig(
        long samplingIntervalMs,          // 采样间隔（毫秒），>= 100，默认 2000
        int maxIterations,                // 最大迭代次数，0 = 无限，默认 100
        int snapshotWindowSize,           // 分类器输入快照窗口大小，>= 2，默认 20
        int oscillationWindowSize,        // 振荡检测窗口（调整次数），>= 4，默认 6
        int oscillationPatternThreshold,  // 触发振荡的最小重复次数，>= 1，默认 2
        int feedbackCalibrationWindow,    // 权重校准窗口（调整次数），>= 5，默认 10
        int emergencyStopThreshold,       // 紧急停止前允许的连续振荡次数，>= 1，默认 2
        List<ThresholdPolicyConfig> candidatePolicies  // 候选策略列表，非空
) {
    public LoopConfig {
        if (samplingIntervalMs < 100) {
            throw new IllegalArgumentException(
                    "samplingIntervalMs must be >= 100, was " + samplingIntervalMs);
        }
        if (maxIterations < 0) {
            throw new IllegalArgumentException(
                    "maxIterations must be >= 0, was " + maxIterations);
        }
        if (snapshotWindowSize < 2) {
            throw new IllegalArgumentException(
                    "snapshotWindowSize must be >= 2, was " + snapshotWindowSize);
        }
        if (oscillationWindowSize < 4) {
            throw new IllegalArgumentException(
                    "oscillationWindowSize must be >= 4, was " + oscillationWindowSize);
        }
        if (oscillationPatternThreshold < 1) {
            throw new IllegalArgumentException(
                    "oscillationPatternThreshold must be >= 1, was " + oscillationPatternThreshold);
        }
        if (feedbackCalibrationWindow < 5) {
            throw new IllegalArgumentException(
                    "feedbackCalibrationWindow must be >= 5, was " + feedbackCalibrationWindow);
        }
        if (emergencyStopThreshold < 1) {
            throw new IllegalArgumentException(
                    "emergencyStopThreshold must be >= 1, was " + emergencyStopThreshold);
        }
        Objects.requireNonNull(candidatePolicies, "candidatePolicies must not be null");
        if (candidatePolicies.isEmpty()) {
            throw new IllegalArgumentException("candidatePolicies must not be empty");
        }
        candidatePolicies = List.copyOf(candidatePolicies);
    }

    public static LoopConfig defaults(List<ThresholdPolicyConfig> candidates) {
        return new LoopConfig(2000, 100, 20, 6, 2, 10, 2, candidates);
    }
}
```

---

### 4.3 LoopSession

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.loop`

**职责**: 一次闭环运行会话的完整信息（不可变）。

```java
public record LoopSession(
        String sessionId,           // UUID
        LoopConfig loopConfig,
        Instant startTime,
        Optional<Instant> endTime,  // Optional.empty() if running
        int adjustmentCount,        // 实际非 NO_OP 调整次数
        int iterationCount,         // 总迭代次数（含 NO_OP/cooldown）
        LoopState finalState,       // 会话结束时的状态
        String summary              // 人类可读摘要
) {
    public LoopSession {
        Objects.requireNonNull(sessionId, "sessionId must not be null");
        if (sessionId.isBlank()) {
            throw new IllegalArgumentException("sessionId must not be blank");
        }
        Objects.requireNonNull(loopConfig, "loopConfig must not be null");
        Objects.requireNonNull(startTime, "startTime must not be null");
        Objects.requireNonNull(finalState, "finalState must not be null");
        if (adjustmentCount < 0) {
            throw new IllegalArgumentException("adjustmentCount must be >= 0");
        }
        if (iterationCount < 0) {
            throw new IllegalArgumentException("iterationCount must be >= 0");
        }
        if (iterationCount < adjustmentCount) {
            throw new IllegalArgumentException(
                    "iterationCount must be >= adjustmentCount");
        }
    }

    /** Create a session that has just started (still running). */
    public static LoopSession started(LoopConfig config) {
        return new LoopSession(
                UUID.randomUUID().toString(),
                config,
                Instant.now(),
                Optional.empty(),
                0, 0,
                LoopState.RUNNING,
                "session started");
    }

    /** Create a copy with end-of-session fields set. */
    public LoopSession ended(LoopState finalState, int adjustments, int iterations, String summary) {
        return new LoopSession(
                this.sessionId, this.loopConfig, this.startTime,
                Optional.of(Instant.now()), adjustments, iterations, finalState, summary);
    }
}
```

---

### 4.4 AdjustmentDecision

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.loop`

**职责**: 单次闭环迭代的决策输出，承载完整的诊断→决策链路。

```java
// 已验证 API 签名:
//   PolicyDecision.action() → PolicyAction (SCALE_UP, SCALE_DOWN, HOLD)
//   PolicyDecision.currentPoolSize() → int, proposedPoolSize() → int
//   PolicyDecision.timestamp() → Instant
//   ScaleAdjustmentCommand.create(runId, timestamp, currentSize, targetSize, reason, ref, clock)
//   ScaleAdjustmentCommand.noOp(runId, timestamp, currentSize, reason, ref, clock)
//   ManagedExecutor.getCurrentPoolSize() → int (via ManagedExecutorAdjustmentAdapter.currentState())

public record AdjustmentDecision(
        PressureClassification classification,
        PolicyScore selectedScore,
        ThresholdPolicyConfig selectedPolicy,
        PolicyDecision policyDecision,
        String rationale,
        Instant decidedAt
) {
    public AdjustmentDecision {
        Objects.requireNonNull(classification, "classification must not be null");
        // selectedScore and selectedPolicy are null ONLY for NO_OP decisions;
        // isNoOp() checks policyDecision.action() == HOLD, not these fields
        Objects.requireNonNull(policyDecision, "policyDecision must not be null");
        Objects.requireNonNull(rationale, "rationale must not be null");
        if (rationale.isBlank()) {
            throw new IllegalArgumentException("rationale must not be blank");
        }
        Objects.requireNonNull(decidedAt, "decidedAt must not be null");
    }

    public boolean isNoOp() {
        return policyDecision.action() == PolicyAction.HOLD;
    }

    /**
     * Convert to ScaleAdjustmentCommand. Caller provides runId (sessionId)
     * and clock for deterministic command id generation.
     */
    public ScaleAdjustmentCommand toCommand(
            ManagedExecutor executor, String runId, Supplier<Instant> clock) {
        if (isNoOp()) {
            return ScaleAdjustmentCommand.noOp(
                    runId,
                    policyDecision.timestamp(),
                    policyDecision.currentPoolSize(),
                    "loop no-op: " + rationale,
                    "loop-decision:" + selectedScore.policyId(),
                    clock);
        }
        return ScaleAdjustmentCommand.create(
                runId,
                policyDecision.timestamp(),
                policyDecision.currentPoolSize(),
                policyDecision.proposedPoolSize(),
                rationale,
                "loop-decision:" + selectedScore.policyId(),
                clock);
    }
}
```

---

### 4.5 TransitionLegality + PressureStateTransition

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.loop`

**职责**: 转换合法性枚举 + 状态转换记录。

```java
public enum TransitionLegality {
    LEGAL,
    ANOMALOUS,   // unexpected but possible (e.g. OVERLOAD→NORMAL skipping RECOVERY)
    ILLEGAL      // logically impossible (e.g. RECOVERY→OVERLOAD)
}

// 已验证 API 签名:
//   PressureState enum values: REJECTION_ACTIVE, OVERLOAD, QUEUE_BUILDUP,
//                              RECOVERY, UNDER_UTILIZED, NORMAL

public record PressureStateTransition(
        PressureState from,
        PressureState to,
        Instant timestamp,
        String trigger,              // e.g. "loop-decision:policy-A"
        TransitionLegality legality
) {
    public PressureStateTransition {
        Objects.requireNonNull(from, "from must not be null");
        Objects.requireNonNull(to, "to must not be null");
        Objects.requireNonNull(timestamp, "timestamp must not be null");
        Objects.requireNonNull(legality, "legality must not be null");
    }
}
```

---

### 4.6 PressureStateMachine

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.loop`

**职责**: 定义压力状态合法转换表，追踪转换历史，查询当前状态。

```java
public final class PressureStateMachine {

    private final List<PressureStateTransition> transitions = new ArrayList<>();

    /** Define legal transition table. */
    private static final Set<Map.Entry<PressureState, PressureState>> LEGAL_TRANSITIONS =
            Set.of(
                    entry(NORMAL, QUEUE_BUILDUP),
                    entry(NORMAL, UNDER_UTILIZED),
                    entry(NORMAL, OVERLOAD),
                    entry(QUEUE_BUILDUP, OVERLOAD),
                    entry(QUEUE_BUILDUP, NORMAL),
                    entry(OVERLOAD, RECOVERY),
                    entry(OVERLOAD, REJECTION_ACTIVE),
                    entry(REJECTION_ACTIVE, RECOVERY),
                    entry(RECOVERY, NORMAL),
                    entry(RECOVERY, UNDER_UTILIZED),
                    entry(UNDER_UTILIZED, NORMAL)
                    // 任意 → NORMAL 总是 LEGAL (在 isLegalTransition 中特殊处理)
            );

    /** Anomalous transitions — unexpected but possible. */
    private static final Set<Map.Entry<PressureState, PressureState>> ANOMALOUS_TRANSITIONS =
            Set.of(
                    entry(OVERLOAD, NORMAL),       // skipped RECOVERY
                    entry(UNDER_UTILIZED, OVERLOAD), // should pass through NORMAL/QUEUE_BUILDUP
                    entry(REJECTION_ACTIVE, NORMAL)   // should pass through RECOVERY
            );

    /** ILLEGAL transitions — logically impossible. */
    private static final Set<Map.Entry<PressureState, PressureState>> ILLEGAL_TRANSITIONS =
            Set.of(
                    entry(RECOVERY, OVERLOAD)       // can't go back to overload during recovery
            );

    public TransitionLegality isLegalTransition(PressureState from, PressureState to) {
        if (from == to) return TransitionLegality.LEGAL;    // same state → always legal
        if (to == PressureState.NORMAL) return TransitionLegality.LEGAL; // any → NORMAL always legal
        var pair = entry(from, to);
        if (LEGAL_TRANSITIONS.contains(pair)) return TransitionLegality.LEGAL;
        if (ANOMALOUS_TRANSITIONS.contains(pair)) return TransitionLegality.ANOMALOUS;
        if (ILLEGAL_TRANSITIONS.contains(pair)) return TransitionLegality.ILLEGAL;
        return TransitionLegality.LEGAL; // unlisted transitions default to LEGAL
    }

    public void recordTransition(
            PressureState from, PressureState to,
            Instant timestamp, String trigger) {
        TransitionLegality legality = isLegalTransition(from, to);
        transitions.add(new PressureStateTransition(from, to, timestamp, trigger, legality));
    }

    public Optional<PressureState> currentState() {
        return transitions.isEmpty()
                ? Optional.empty()
                : Optional.of(transitions.get(transitions.size() - 1).to());
    }

    public List<PressureStateTransition> recentTransitions(int count) {
        int from = Math.max(0, transitions.size() - count);
        return List.copyOf(transitions.subList(from, transitions.size()));
    }

    public int transitionCount() {
        return transitions.size();
    }

    /** Reset — clear all history. Called by AdjustmentLoop.reset(). */
    public void reset() {
        transitions.clear();
    }

    private static Map.Entry<PressureState, PressureState> entry(
            PressureState from, PressureState to) {
        return Map.entry(from, to);
    }
}
```

**设计决策**:
- 使用 `Set<Map.Entry<>>` 定义合法/异常/非法转换，O(1) 查找
- "任意 → NORMAL" 总是 LEGAL（任何状态都可以回到稳态）
- 未在 LEGAL/ANOMALOUS/ILLEGAL 中列出的转换默认 LEGAL（开放世界假设）
- `TransitionLegality` 是信息性的——ANOMALOUS 不阻塞闭环（但降置信度），ILLEGAL 记录警告
- 状态机的 `reset()` 由 `AdjustmentLoop.reset()` 触发

---

### 4.7 AdjustmentHistory + HistoryEntry

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.loop`

**职责**: 存储和查询调整记录，计算成功率。

```java
// 已验证 API 签名:
//   AdjustmentResult.status() → AdjustmentStatus (APPLIED, REJECTED, FAILED, NO_OP, DEFERRED)

public record HistoryEntry(
        AdjustmentDecision decision,
        AdjustmentResult result,
        PressureClassification beforeClassification,
        PressureClassification afterClassification,
        Instant recordedAt
) {
    public HistoryEntry {
        Objects.requireNonNull(decision, "decision must not be null");
        Objects.requireNonNull(result, "result must not be null");
        Objects.requireNonNull(beforeClassification, "beforeClassification must not be null");
        Objects.requireNonNull(afterClassification, "afterClassification must not be null");
        Objects.requireNonNull(recordedAt, "recordedAt must not be null");
    }
}

public final class AdjustmentHistory {

    // Thread-safe: use CopyOnWriteArrayList to support concurrent record()/recent() calls
    private final List<HistoryEntry> entries = new CopyOnWriteArrayList<>();

    public void record(AdjustmentDecision decision, AdjustmentResult result,
                       PressureClassification before, PressureClassification after) {
        entries.add(new HistoryEntry(decision, result, before, after, Instant.now()));
    }

    public List<HistoryEntry> recent(int count) {
        int from = Math.max(0, entries.size() - count);
        return List.copyOf(entries.subList(from, entries.size()));
    }

    public List<HistoryEntry> since(Instant timestamp) {
        return entries.stream()
                .filter(e -> e.recordedAt().isAfter(timestamp))
                .toList();
    }

    public int totalAdjustmentCount() {
        return entries.size();
    }

    public int successfulAdjustmentCount() {
        return (int) entries.stream()
                .filter(e -> isImprovement(
                        e.beforeClassification().state(),
                        e.afterClassification().state()))
                .count();
    }

    public boolean isEmpty() {
        return entries.isEmpty();
    }

    public void clear() {
        entries.clear();
    }

    /**
     * Determine if the pressure state improved or stayed stable.
     * LOW priority = worse state; HIGH priority = better state.
     * ordinal: REJECTION_ACTIVE=0, OVERLOAD=1, QUEUE_BUILDUP=2,
     *          RECOVERY=3, UNDER_UTILIZED=4, NORMAL=5
     * higher ordinal = lower pressure → improvement if after > before.
     * Exception: NORMAL→NORMAL = success (stable)
     */
    static boolean isImprovement(PressureState before, PressureState after) {
        if (before == after && after == PressureState.NORMAL) return true;
        return after.ordinal() > before.ordinal();
    }
}
```

**设计决策**:
- `CopyOnWriteArrayList` 保证线程安全——`AdjustmentLoop` 循环线程写入，`FeedbackCalibrator` 读取
- `isImprovement` 基于 `PressureState` 枚举序（高序 = 低压力 = 改善）
- `NORMAL→NORMAL` 也算成功（维持稳态）
- `clear()` 由 `AdjustmentLoop.reset()` 调用

---

### 4.8 OscillationDetector

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.loop`

**职责**: 滑动窗口振荡模式检测——纯函数，无内部状态。

```java
public final class OscillationDetector {

    private final int windowSize;
    private final int patternThreshold;

    public OscillationDetector(int windowSize, int patternThreshold) {
        if (windowSize < 4) {
            throw new IllegalArgumentException("windowSize must be >= 4, was " + windowSize);
        }
        if (patternThreshold < 1) {
            throw new IllegalArgumentException("patternThreshold must be >= 1, was " + patternThreshold);
        }
        this.windowSize = windowSize;
        this.patternThreshold = patternThreshold;
    }

    public OscillationDetector() {
        this(6, 2);
    }

    /**
     * Check if adding {@code pending} to history would trigger an oscillation pattern.
     *
     * @param pending the decision about to be executed
     * @param history the adjustment history (may be empty)
     * @return true if executing pending would form an oscillation pattern
     */
    public boolean wouldOscillate(AdjustmentDecision pending, AdjustmentHistory history) {
        List<HistoryEntry> recent = history.recent(windowSize);
        if (recent.size() < windowSize - 1) return false; // not enough data

        // Extract target pool sizes from history + pending
        List<Integer> targets = new ArrayList<>();
        for (HistoryEntry e : recent) {
            if (!e.decision().isNoOp()) {
                targets.add(e.decision().policyDecision().proposedPoolSize());
            }
        }
        if (!pending.isNoOp()) {
            targets.add(pending.policyDecision().proposedPoolSize());
        }

        if (targets.size() < 4) return false;

        // Check ping-pong: alternating direction changes >= patternThreshold
        if (detectPingPong(targets)) return true;

        // Check over-adjustment: consecutive same direction >= 3
        if (detectOverAdjustment(targets)) return true;

        // Check policy switching: frequent policy changes
        if (detectPolicySwitching(recent, pending)) return true;

        return false;
    }

    private boolean detectPingPong(List<Integer> targets) {
        int directionChanges = 0;
        Integer prevDiff = null;
        for (int i = 1; i < targets.size(); i++) {
            int diff = targets.get(i) - targets.get(i - 1);
            if (diff == 0) continue;
            int sign = diff > 0 ? 1 : -1;
            if (prevDiff != null && sign != prevDiff) {
                directionChanges++;
            }
            prevDiff = sign;
        }
        return directionChanges >= patternThreshold;
    }

    private boolean detectOverAdjustment(List<Integer> targets) {
        int consecutiveSame = 1;
        for (int i = 2; i < targets.size(); i++) {
            int diff1 = targets.get(i - 1) - targets.get(i - 2);
            int diff2 = targets.get(i) - targets.get(i - 1);
            if (diff1 != 0 && diff2 != 0
                    && Integer.signum(diff1) == Integer.signum(diff2)) {
                consecutiveSame++;
                if (consecutiveSame >= 3) return true;
            } else {
                consecutiveSame = 1;
            }
        }
        return false;
    }

    private boolean detectPolicySwitching(List<HistoryEntry> recent, AdjustmentDecision pending) {
        Set<String> policies = new HashSet<>();
        Map<String, Integer> policyCounts = new HashMap<>();
        for (HistoryEntry e : recent) {
            String pid = e.decision().selectedPolicy().policyId();
            policies.add(pid);
            policyCounts.merge(pid, 1, Integer::sum);
        }
        String pendingPid = pending.selectedPolicy().policyId();
        policies.add(pendingPid);
        policyCounts.merge(pendingPid, 1, Integer::sum);

        if (policies.size() <= 1) return false;
        // A policy appears >= 2 times separated by other policies → switching oscillation
        return policyCounts.values().stream().anyMatch(c -> c >= 2)
                && policies.size() > 1;
    }

    public Optional<String> detectedPattern(AdjustmentHistory history) {
        List<HistoryEntry> recent = history.recent(windowSize);
        if (recent.isEmpty()) return Optional.empty();

        List<Integer> targets = recent.stream()
                .filter(e -> !e.decision().isNoOp())
                .map(e -> e.decision().policyDecision().proposedPoolSize())
                .toList();

        if (detectPingPong(targets)) return Optional.of("ping-pong oscillation");
        if (detectOverAdjustment(targets)) return Optional.of("over-adjustment");
        // policy switching check without pending
        if (recent.size() >= 4 && detectPolicySwitching(recent.subList(0, recent.size() - 1),
                recent.get(recent.size() - 1).decision())) {
            return Optional.of("policy switching oscillation");
        }

        return Optional.empty();
    }
}
```

**设计决策**:
- 无状态纯函数——输入历史 + 待处理决策 → 输出 boolean
- `pingPong` 检测方向交替次数（非 poolSize 绝对值）
- `overAdjustment` 检测连续 3 次同方向
- `policySwitching` 检测窗口内同一 policy 被选中 2+ 次且被其他策略隔开
- `patternThreshold` = 1 时每次方向变化都算振荡（最敏感）；= 2 时（默认）需要交替 2 次

---

### 4.9 DecisionOrchestrator

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.loop`

**职责**: 编排 classification → ranking → evaluation 的完整决策链。

```java
// 已验证 API 签名:
//   PressureClassifier.classify(List<ObservedSnapshot>, ClassifierConfig, long rejectedTaskCount, long totalDurationMs) → PressureClassification
//   PolicyRanker.rank(PressureClassification, List<ThresholdPolicyConfig>) → List<PolicyScore>
//   ObservedSnapshot.snapshot() → PressureSnapshot (含 activeThreads, poolSize, queueSize, etc.)
//   PolicyEvaluationInput(String runId, PressureSnapshot snapshot, Instant evaluatedAt)
//   PolicyEvaluator.evaluate(PolicyEvaluationInput, ThresholdPolicyConfig) → PolicyDecision
//   ManagedExecutor.getRejectedTaskCount() → long

public final class DecisionOrchestrator {

    private final PressureClassifier classifier;
    private final PolicyRanker ranker;
    private final PolicyEvaluator evaluator;
    private final ClassifierConfig classifierConfig;

    public DecisionOrchestrator(
            PressureClassifier classifier,
            PolicyRanker ranker,
            PolicyEvaluator evaluator,
            ClassifierConfig classifierConfig) {
        this.classifier = Objects.requireNonNull(classifier, "classifier must not be null");
        this.ranker = Objects.requireNonNull(ranker, "ranker must not be null");
        this.evaluator = Objects.requireNonNull(evaluator, "evaluator must not be null");
        this.classifierConfig = Objects.requireNonNull(classifierConfig, "classifierConfig must not be null");
    }

    // DecisionOrchestrator is immutable. Weight updates after calibration
    // are handled by AdjustmentLoop creating a new orchestrator instance:
    //   this.orchestrator = new DecisionOrchestrator(
    //       classifier, new PolicyRanker(newScorer), evaluator, classifierConfig);

    /**
     * Execute the full decision pipeline.
     *
     * @param snapshots  recent snapshots (non-empty)
     * @param candidates candidate policy configs
     * @param executor   managed executor (for rejectedTaskCount)
     * @param runId      run identifier (sessionId)
     * @return AdjustmentDecision (never null; may be no-op)
     */
    public AdjustmentDecision decide(
            List<ObservedSnapshot> snapshots,
            List<ThresholdPolicyConfig> candidates,
            ManagedExecutor executor,
            String runId) {

        if (snapshots.isEmpty()) {
            return createNoOpDecision("empty snapshots", runId);
        }

        // Step 1: compute total duration from first/last snapshot timestamps
        long firstTs = snapshots.get(0).snapshot().timestamp().toEpochMilli();
        long lastTs = snapshots.get(snapshots.size() - 1).snapshot().timestamp().toEpochMilli();
        long totalDurationMs = Math.max(0, lastTs - firstTs);

        // Step 2: classify
        long rejectedTaskCount = executor.getRejectedTaskCount();
        // 已验证: ManagedExecutor.getRejectedTaskCount() → long (v0.12.0)
        PressureClassification classification = classifier.classify(
                snapshots, classifierConfig, rejectedTaskCount, totalDurationMs);

        // Step 3: rank policies
        List<PolicyScore> ranked = ranker.rank(classification, candidates);
        if (ranked.isEmpty()) {
            return createNoOpDecision("no candidates scored", runId);
        }
        PolicyScore bestScore = ranked.get(0);

        // Step 4: find the corresponding config for the best score
        ThresholdPolicyConfig selectedConfig = candidates.stream()
                .filter(c -> c.policyId().equals(bestScore.policyId()))
                .findFirst()
                .orElse(candidates.get(0));

        // Step 5: construct PolicyEvaluationInput from last snapshot
        PressureSnapshot lastSnapshot = snapshots.get(snapshots.size() - 1).snapshot();
        // 已验证: ObservedSnapshot.snapshot() → PressureSnapshot
        // Use snapshot timestamp for reproducibility (not wall-clock)
        PolicyEvaluationInput input = new PolicyEvaluationInput(
                runId, lastSnapshot, lastSnapshot.timestamp());

        // Step 6: evaluate selected policy
        PolicyDecision decision = evaluator.evaluate(input, selectedConfig);

        // Step 7: assemble
        String rationale = String.format(
                "[%s] Selected policy '%s' (composite=%.2f): %s",
                classification.state().name(),
                bestScore.policyId(),
                bestScore.compositeScore(),
                decision.reason());

        return new AdjustmentDecision(
                classification, bestScore, selectedConfig, decision, rationale, Instant.now());
    }

    private AdjustmentDecision createNoOpDecision(String reason, String runId) {
        return new AdjustmentDecision(
                new PressureClassification(PressureState.NORMAL, 0.0, List.of(),
                        NormalizedPressureMetrics.fromSnapshots(List.of(), 0L, 0, 5),
                        Instant.now()),
                null, // selectedScore — null for NO_OP
                null, // selectedPolicy
                new PolicyDecision(runId, "no-op", Instant.now(), PolicyAction.HOLD,
                        GateStatus.HOLD, 0, 0, reason),
                reason,
                Instant.now());
    }
}
```

**设计决策**:
- `DecisionOrchestrator` 完全不可变。权重更新通过 `AdjustmentLoop` 重建 orchestrator 实例实现
- NO_OP 决策使用 sentinel 值（selectedScore=null, selectedPolicy=null, action=HOLD）
- `runId` 由 `AdjustmentLoop` 从 `session.sessionId()` 提供
- 不持有 `ManagedExecutor` 引用——`executor` 由 `decide()` 方法参数传入

---

### 4.10 AdjustmentLoop

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.loop`

**职责**: 闭环主控制器——管理生命周期，执行主循环。

```java
// 已验证 API 签名:
//   RuntimeAdjustmentSafetyGate.evaluate(ScaleAdjustmentCommand, ExecutorStateSnapshot, ReadinessAssessment) → SafetyGateDecision
//   SafetyGateDecision.outcome() → Outcome (ALLOW, REJECTED, NO_OP)
//   SafetyGateDecision.Outcome.ALLOW, REJECTED, NO_OP
//   RuntimeAdjustmentSafetyGate.recordApplied(SafetyGateDecision)
//   ExecutorAdjustmentAdapter.currentState() → ExecutorStateSnapshot
//   ExecutorAdjustmentAdapter.apply(ScaleAdjustmentCommand) → AdjustmentResult
//   EvidenceRecorder.snapshots(String runId) → List<ObservedSnapshot>
//   ReadinessAssessment(ReadinessStatus, List<ScenarioProfile>, List<ScenarioProfile>,
//                       List<String>, List<String>, String, List<String>)
//   ReadinessStatus.READY

public final class AdjustmentLoop {

    private final LoopConfig config;
    private volatile DecisionOrchestrator orchestrator;  // non-final: replaced on calibration
    // References needed for orchestrator rebuild after calibration:
    private final PressureClassifier classifier;
    private final PolicyEvaluator evaluator;
    private final ClassifierConfig classifierConfig;
    private final ExecutorAdjustmentAdapter adapter;
    private final RuntimeAdjustmentSafetyGate safetyGate;
    private final OscillationDetector oscillationDetector;
    private final AdjustmentHistory history;
    private final LoopEvidenceRecorder loopEvidenceRecorder;
    private final PressureStateMachine stateMachine;
    private final EvidenceRecorder evidenceRecorder;  // for snapshot queries
    private final FeedbackCalibrator calibrator;

    private LoopState state = LoopState.IDLE;
    private LoopSession currentSession;
    private Thread loopThread;
    private int consecutiveOscillations;

    // Wait/notify for pause/resume
    private final Object pauseLock = new Object();
    private volatile boolean paused = false;

    public AdjustmentLoop(
            LoopConfig config,
            DecisionOrchestrator orchestrator,
            ExecutorAdjustmentAdapter adapter,
            RuntimeAdjustmentSafetyGate safetyGate,
            OscillationDetector oscillationDetector,
            AdjustmentHistory history,
            LoopEvidenceRecorder loopEvidenceRecorder,
            PressureStateMachine stateMachine,
            EvidenceRecorder evidenceRecorder,
            FeedbackCalibrator calibrator) {
        this.config = Objects.requireNonNull(config, "config must not be null");
        this.orchestrator = Objects.requireNonNull(orchestrator, "orchestrator must not be null");
        this.adapter = Objects.requireNonNull(adapter, "adapter must not be null");
        this.safetyGate = Objects.requireNonNull(safetyGate, "safetyGate must not be null");
        this.oscillationDetector = Objects.requireNonNull(oscillationDetector, "oscillationDetector must not be null");
        this.history = Objects.requireNonNull(history, "history must not be null");
        this.loopEvidenceRecorder = Objects.requireNonNull(loopEvidenceRecorder, "loopEvidenceRecorder must not be null");
        this.stateMachine = Objects.requireNonNull(stateMachine, "stateMachine must not be null");
        this.evidenceRecorder = Objects.requireNonNull(evidenceRecorder, "evidenceRecorder must not be null");
        this.calibrator = Objects.requireNonNull(calibrator, "calibrator must not be null");
    }

    // --- Lifecycle methods ---

    public LoopSession start(ManagedExecutor executor) {
        if (state != LoopState.IDLE) {
            throw new IllegalStateException("Cannot start: state is " + state);
        }
        state = LoopState.RUNNING;
        paused = false;
        consecutiveOscillations = 0;

        currentSession = LoopSession.started(config);
        loopEvidenceRecorder.recordSessionStart(currentSession);

        loopThread = new Thread(() -> runLoop(executor),
                "adjustment-loop-" + currentSession.sessionId());
        loopThread.setDaemon(true);
        loopThread.start();

        return currentSession;
    }

    public void pause() {
        if (state != LoopState.RUNNING) {
            throw new IllegalStateException("Cannot pause: state is " + state);
        }
        state = LoopState.PAUSED;
        paused = true;
    }

    public void resume() {
        if (state != LoopState.PAUSED) {
            throw new IllegalStateException("Cannot resume: state is " + state);
        }
        state = LoopState.RUNNING;
        paused = false;
        synchronized (pauseLock) {
            pauseLock.notifyAll();
        }
    }

    public LoopSession stop() {
        if (state != LoopState.RUNNING && state != LoopState.PAUSED) {
            throw new IllegalStateException("Cannot stop: state is " + state);
        }
        state = LoopState.STOPPED;
        unpauseAndInterrupt();
        return finalizeSession(LoopState.STOPPED, "normal stop");
    }

    public LoopSession emergencyStop(String reason) {
        if (state != LoopState.RUNNING && state != LoopState.PAUSED) {
            throw new IllegalStateException("Cannot emergency stop: state is " + state);
        }
        state = LoopState.EMERGENCY_STOPPED;
        unpauseAndInterrupt();
        return finalizeSession(LoopState.EMERGENCY_STOPPED,
                "emergency stop: " + reason);
    }

    public void reset() {
        if (state != LoopState.STOPPED && state != LoopState.EMERGENCY_STOPPED) {
            throw new IllegalStateException("Cannot reset: state is " + state);
        }
        state = LoopState.IDLE;
        history.clear();
        stateMachine.reset();
        currentSession = null;
        loopThread = null;
        consecutiveOscillations = 0;
    }

    public LoopState getState() { return state; }
    public LoopSession getCurrentSession() { return currentSession; }
    public AdjustmentHistory getHistory() { return history; }

    // --- Private ---

    private void runLoop(ManagedExecutor executor) {
        PressureClassification previousClassification = null;
        int iteration = 0;

        // Construct runtime ReadinessAssessment (always READY — runtime loop)
        ReadinessAssessment loopReadiness = new ReadinessAssessment(
                ReadinessStatus.READY,
                List.of(), List.of(), List.of(), List.of(),
                "runtime-loop",
                List.of(currentSession.sessionId()));

        while (state == LoopState.RUNNING || state == LoopState.PAUSED) {
            // Handle pause
            if (paused) {
                synchronized (pauseLock) {
                    try { pauseLock.wait(config.samplingIntervalMs); } catch (InterruptedException e) { break; }
                }
                continue;
            }

            iteration++;

            try {
                // Step 1: sleep for sampling interval
                Thread.sleep(config.samplingIntervalMs());

                // Step 2: get recent snapshots
                List<ObservedSnapshot> allSnapshots = evidenceRecorder.snapshots(
                        currentSession.sessionId());
                int from = Math.max(0, allSnapshots.size() - config.snapshotWindowSize());
                List<ObservedSnapshot> recent = allSnapshots.subList(from, allSnapshots.size());
                if (recent.isEmpty()) continue;

                // Step 3: decide
                AdjustmentDecision decision = orchestrator.decide(
                        recent, config.candidatePolicies(),
                        executor, currentSession.sessionId());

                // Step 4: skip no-op
                if (decision.isNoOp()) {
                    loopEvidenceRecorder.recordIteration(currentSession, iteration,
                            decision, null, previousClassification);
                    continue;
                }

                // Step 5-6: oscillation check
                if (oscillationDetector.wouldOscillate(decision, history)) {
                    consecutiveOscillations++;
                    if (consecutiveOscillations >= config.emergencyStopThreshold()) {
                        emergencyStop("oscillation detected: "
                                + oscillationDetector.detectedPattern(history).orElse("unknown"));
                        break;
                    }
                    // Record rejected due to oscillation
                    continue;
                }
                consecutiveOscillations = 0; // reset on clean iteration

                // Step 7: build command
                ScaleAdjustmentCommand command = decision.toCommand(
                        executor, currentSession.sessionId(), Instant::now);

                // Step 8: safety gate
                ExecutorStateSnapshot executorState = adapter.currentState();
                SafetyGateDecision gateDecision = safetyGate.evaluate(
                        command, executorState, loopReadiness);

                // Step 9: check gate outcome
                if (gateDecision.outcome() == SafetyGateDecision.Outcome.REJECTED) {
                    loopEvidenceRecorder.recordIteration(currentSession, iteration,
                            decision, null, previousClassification);
                    continue;
                }
                if (gateDecision.outcome() == SafetyGateDecision.Outcome.NO_OP) {
                    continue;
                }

                // Step 10: apply
                AdjustmentResult result = adapter.apply(command);

                // Step 11: record applied → SafetyGate cooldown
                safetyGate.recordApplied(gateDecision);

                // Step 12: record in history with correct before/after
                // before = previous iteration's classification, after = current
                // first iteration: before == after (no previous data)
                PressureClassification beforeClass = previousClassification;
                PressureClassification afterClass = decision.classification();
                history.record(decision, result,
                        beforeClass != null ? beforeClass : afterClass,
                        afterClass);
                previousClassification = afterClass;

                // Step 13: state transition tracking
                if (previousClassification != null) {
                    stateMachine.recordTransition(
                            previousClassification.state(),
                            decision.classification().state(),
                            Instant.now(),
                            "loop-decision:" + decision.selectedScore().policyId());
                }
                previousClassification = decision.classification();

                // Step 14: evidence recording
                loopEvidenceRecorder.recordIteration(currentSession, iteration,
                        decision, result, previousClassification);

                // Step 15: feedback calibration
                if (history.totalAdjustmentCount() > 0
                        && history.totalAdjustmentCount() % config.feedbackCalibrationWindow() == 0) {
                    ThresholdPolicyScorer currentScorer = getScorerFromOrchestrator();
                    ThresholdPolicyScorer newScorer = calibrator.calibrate(
                            history, currentScorer, config.feedbackCalibrationWindow());
                    if (newScorer != currentScorer) {
                        this.orchestrator = new DecisionOrchestrator(
                                classifier, new PolicyRanker(newScorer),
                                evaluator, classifierConfig);
                    }
                }

                // Step 16: max iterations check
                if (config.maxIterations() > 0 && iteration >= config.maxIterations()) {
                    stop();
                    break;
                }

            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
                break;
            } catch (RuntimeException e) {
                // Log and continue — don't let one iteration crash the loop
                loopEvidenceRecorder.recordIteration(currentSession, iteration,
                        null, null, previousClassification);
            }
        }

        // Loop ended — finalize session if not already finalized
        if (currentSession != null && currentSession.endTime().isEmpty()) {
            finalizeSession(state, "loop exited");
        }
    }

    private LoopSession finalizeSession(LoopState finalState, String summary) {
        loopEvidenceRecorder.recordSessionEnd(currentSession);
        int adjustments = history.totalAdjustmentCount();
        int iterations = 0; // track iteration counter
        currentSession = currentSession.ended(finalState, adjustments, iterations, summary);
        return currentSession;
    }

    private void unpauseAndInterrupt() {
        paused = false;
        synchronized (pauseLock) {
            pauseLock.notifyAll();
        }
        if (loopThread != null) {
            loopThread.interrupt();
        }
    }
}
```

**设计说明**:
- `afterClassification` 在当前迭代中无法获取（需要下次迭代的分类结果）。设计选择：当前迭代记录 `afterClassification = beforeClassification`（占位），下一次迭代开始前用真正的分类结果更新上一条 HistoryEntry 的 `afterClassification`。或者实现为：每次迭代在 classify 之后、record 之前更新上一条 entry。Implementation 阶段选择具体方案
- `FeedbackCalibrator` 的触发在主循环步骤 15 中标注为 callback/外部触发——实际触发由实现决定（可在 calibrator 的 trigger 方法中检查 `history.totalAdjustmentCount() % window == 0`）
- `iteration` 计数器在 stop/emergencyStop 时传给 `LoopSession.ended()`

---

### 4.11 FeedbackCalibrator

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.loop`

**职责**: 基于调整历史校准评分权重。

```java
// 依赖: ThresholdPolicyScorer 新增包级可见权重 getter (experiment.classification 包内)
//   double wResponsiveness(), double wSafety(), double wStability(), double wEfficiency()

public final class FeedbackCalibrator {

    private final double maxAdjustmentPerCycle;
    private final double minWeight;
    private final double maxWeight;

    public FeedbackCalibrator(double maxAdjustmentPerCycle, double minWeight, double maxWeight) {
        if (maxAdjustmentPerCycle <= 0 || maxAdjustmentPerCycle > 0.2) {
            throw new IllegalArgumentException(
                    "maxAdjustmentPerCycle must be in (0, 0.2], was " + maxAdjustmentPerCycle);
        }
        if (minWeight < 0.05 || minWeight > 0.2) {
            throw new IllegalArgumentException(
                    "minWeight must be in [0.05, 0.20], was " + minWeight);
        }
        if (maxWeight < 0.3 || maxWeight > 0.6) {
            throw new IllegalArgumentException(
                    "maxWeight must be in [0.30, 0.60], was " + maxWeight);
        }
        this.maxAdjustmentPerCycle = maxAdjustmentPerCycle;
        this.minWeight = minWeight;
        this.maxWeight = maxWeight;
    }

    public FeedbackCalibrator() {
        this(0.05, 0.10, 0.50);
    }

    /**
     * Calibrate weights based on adjustment history.
     *
     * @param history       adjustment history
     * @param currentScorer current scorer (for reading weights via getters)
     * @param windowSize    number of recent adjustments to analyze
     * @return new ThresholdPolicyScorer with calibrated weights,
     *         or currentScorer if insufficient data
     */
    public ThresholdPolicyScorer calibrate(
            AdjustmentHistory history,
            ThresholdPolicyScorer currentScorer,
            int windowSize) {

        List<HistoryEntry> recent = history.recent(windowSize);
        if (recent.size() < windowSize) {
            return currentScorer; // insufficient data
        }

        // Read current weights via package-visible getters
        double wR = currentScorer.wResponsiveness();
        double wS = currentScorer.wSafety();
        double wSt = currentScorer.wStability();
        double wE = currentScorer.wEfficiency();

        // Calculate correlation for each dimension
        wR = adjustWeight(wR, correlation(recent, DIM_RESPONSIVENESS));
        wS = adjustWeight(wS, correlation(recent, DIM_SAFETY));
        wSt = adjustWeight(wSt, correlation(recent, DIM_STABILITY));
        wE = adjustWeight(wE, correlation(recent, DIM_EFFICIENCY));

        // Normalize
        double sum = wR + wS + wSt + wE;
        wR /= sum; wS /= sum; wSt /= sum; wE /= sum;

        // Clamp
        wR = clamp(wR); wS = clamp(wS); wSt = clamp(wSt); wE = clamp(wE);

        // Re-normalize after clamping
        sum = wR + wS + wSt + wE;
        wR /= sum; wS /= sum; wSt /= sum; wE /= sum;

        return new ThresholdPolicyScorer(wR, wS, wSt, wE);
    }

    // Dimension extractors
    private static final Function<PolicyScore, Double> DIM_RESPONSIVENESS = PolicyScore::responsivenessScore;
    private static final Function<PolicyScore, Double> DIM_SAFETY = PolicyScore::safetyScore;
    private static final Function<PolicyScore, Double> DIM_STABILITY = PolicyScore::stabilityScore;
    private static final Function<PolicyScore, Double> DIM_EFFICIENCY = PolicyScore::efficiencyScore;

    private double correlation(List<HistoryEntry> entries,
                               Function<PolicyScore, Double> dimensionExtractor) {
        // Split entries by median score on this dimension
        List<Double> scores = entries.stream()
                .map(e -> dimensionExtractor.apply(e.decision().selectedScore()))
                .sorted()
                .toList();
        double median = scores.get(scores.size() / 2);

        long highSuccess = 0, highTotal = 0;
        long lowSuccess = 0, lowTotal = 0;

        for (HistoryEntry e : entries) {
            double score = dimensionExtractor.apply(e.decision().selectedScore());
            boolean success = AdjustmentHistory.isImprovement(
                    e.beforeClassification().state(),
                    e.afterClassification().state());
            if (score >= median) {
                highTotal++;
                if (success) highSuccess++;
            } else {
                lowTotal++;
                if (success) lowSuccess++;
            }
        }

        double highRate = highTotal > 0 ? (double) highSuccess / highTotal : 0.5;
        double lowRate = lowTotal > 0 ? (double) lowSuccess / lowTotal : 0.5;

        return highRate - lowRate; // [-1.0, 1.0]
    }

    private double adjustWeight(double currentWeight, double correlation) {
        if (correlation > 0.1) return currentWeight + maxAdjustmentPerCycle;
        if (correlation < -0.1) return currentWeight - maxAdjustmentPerCycle;
        return currentWeight;
    }

    private double clamp(double w) {
        return Math.max(minWeight, Math.min(maxWeight, w));
    }
}
```

**设计决策**:
- `calibrate()` 返回新 `ThresholdPolicyScorer` 实例——不修改传入的 scorer
- 依赖 `ThresholdPolicyScorer` 新增的 4 个包级可见 getter（`wResponsiveness()` 等）
- `correlation` 使用中位数分割法（高分/低分组的成功率差），而非 Pearson 相关系数
- 窗口不足时不校准（返回 currentScorer）
- `AdjustmentHistory.isImprovement()` 必须是包级可见或公共方法（供 calibrator 调用）

---

### 4.12 ThresholdPolicyScorer 修改

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.classification`

**修改**: 添加 4 个包级可见权重 getter。

```java
// 在 ThresholdPolicyScorer.java 中添加以下方法:

/**
 * Returns the responsiveness weight (package-visible for FeedbackCalibrator).
 */
double wResponsiveness() { return wResponsiveness; }

/**
 * Returns the safety weight (package-visible for FeedbackCalibrator).
 */
double wSafety() { return wSafety; }

/**
 * Returns the stability weight (package-visible for FeedbackCalibrator).
 */
double wStability() { return wStability; }

/**
 * Returns the efficiency weight (package-visible for FeedbackCalibrator).
 */
double wEfficiency() { return wEfficiency; }
```

**侵入性评估**: 4 个方法，总计 4 行。不影响现有 public API。不修改任何现有测试。

---

### 4.13 LoopEvidenceRecorder + LoopIterationEvidence

**包**: `com.zhiwu.dynamicthreadpollermanager.experiment.loop`

**职责**: 闭环迭代证据记录接口。

```java
public interface LoopEvidenceRecorder {

    void recordIteration(LoopSession session, int iterationIndex,
                         AdjustmentDecision decision, AdjustmentResult result,
                         PressureClassification beforeClassification);

    void recordSessionStart(LoopSession session);

    void recordSessionEnd(LoopSession session);

    List<LoopIterationEvidence> getIterationEvidence(String sessionId);
}

public record LoopIterationEvidence(
        String sessionId,
        int iterationIndex,
        AdjustmentDecision decision,
        AdjustmentResult result,                       // null for NO_OP/REJECTED iterations
        PressureClassification beforeClassification,   // null for first iteration
        Instant recordedAt
) {
    public LoopIterationEvidence {
        Objects.requireNonNull(sessionId, "sessionId must not be null");
        if (sessionId.isBlank()) throw new IllegalArgumentException("sessionId must not be blank");
        if (iterationIndex < 0) throw new IllegalArgumentException("iterationIndex must be >= 0");
        Objects.requireNonNull(decision, "decision must not be null");
        Objects.requireNonNull(recordedAt, "recordedAt must not be null");
    }
}
```

**Change 1 使用 mock/stub 实现**，Change 2 提供 `FileBackedLoopEvidenceRecorder`（委托到 `FileBackedEvidenceRecorder`）。

---

## 5. 并发设计

| 组件 | 线程访问 | 策略 |
|---|---|---|
| `AdjustmentHistory.entries` | loop thread 写入, calibrator 读取 | `CopyOnWriteArrayList` |
| `PressureStateMachine.transitions` | loop thread 独占写入 | `ArrayList` + 调用方保证单线程 |
| `AdjustmentLoop.state` | loop thread + 控制线程 (start/pause/stop) | `volatile` 或 `synchronized` |
| `AdjustmentLoop.paused` | loop thread + 控制线程 | `volatile` |
| `EvidenceRecorder` | sampler thread 写入, loop thread 读取 | 见风险：`InMemoryEvidenceRecorder` 使用 `ArrayList` — 实现必须用线程安全方案 |
| `DecisionOrchestrator` | loop thread 独占 | 无状态，无需同步 |

**快照并发安全**（IR F06/F12 处置）:
- 如果使用 `InMemoryEvidenceRecorder`：实现必须将内部 `ArrayList` 替换为 `CopyOnWriteArrayList`，或使用显式 `synchronized` 保护
- 如果使用 `FileBackedEvidenceRecorder`：文件追加写入是原子的，读取通过文件重读——天然线程安全
- `AdjustmentLoop` 读取 `evidenceRecorder.snapshots(runId)` 取最近 N 个快照的 subList 副本

---

## 6. 安全设计

| 安全机制 | 实现方 | 触发条件 | 行为 |
|---|---|---|---|
| 冷却期 | SafetyGate | `cooldownRemaining > 0` → REJECTED | 跳过调整，continue |
| 边界校验 | SafetyGate | targetPoolSize < 1, currentPoolSize < 0 → REJECTED | 跳过调整，continue |
| 方向保护 | SafetyGate | immediate opposite direction → REJECTED | 跳过调整，continue |
| 运行上限 | SafetyGate | per-run adjustment count exceeded → REJECTED | 跳过调整，continue |
| 振荡检测 | OscillationDetector | 连续 N 次振荡模式 | EMERGENCY_STOPPED |
| 最大迭代 | AdjustmentLoop | maxIterations > 0 && iteration >= maxIterations | STOPPED |
| loop 异常 | AdjustmentLoop | RuntimeException per iteration | 记录并 continue（不崩溃） |
| InterruptedException | AdjustmentLoop | 线程被中断 | 正常退出循环 |

---

## 7. 测试映射

| IR AC | 测试类型 | 测试目标 |
|---|---|---|
| AC-v0.14-001 | 单元 | LoopState 5 枚举值 + 合法转换 |
| AC-v0.14-002 | 单元 | LoopConfig 构造验证 + defaults() |
| AC-v0.14-003 | 单元 | AdjustmentLoop 生命周期：IDLE→RUNNING→PAUSED→STOPPED |
| AC-v0.14-004 | 单元 | AdjustmentLoop 非法转换抛 IllegalStateException |
| AC-v0.14-005 | 单元 | NO_OP 决策跳过调整 |
| AC-v0.14-006 | 单元 | SafetyGate REJECTED 时 continue |
| AC-v0.14-007 | 单元 | maxIterations 达到 → STOPPED |
| AC-v0.14-008 | 单元 | DecisionOrchestrator 空快照 → NO_OP |
| AC-v0.14-009 | 单元 | DecisionOrchestrator OVERLOAD → 激进策略 |
| AC-v0.14-010 | 单元 | AdjustmentDecision.isNoOp() |
| AC-v0.14-011 | 单元 | AdjustmentDecision.toCommand() |
| AC-v0.14-012 | 单元 | PressureStateMachine 合法转换 = LEGAL |
| AC-v0.14-013 | 单元 | PressureStateMachine ANOMALOUS 转换 |
| AC-v0.14-014 | 单元 | PressureStateMachine ILLEGAL 转换 |
| AC-v0.14-015 | 单元 | PressureStateMachine currentState/recentTransitions |
| AC-v0.14-016 | 单元 | OscillationDetector 乒乓振荡 |
| AC-v0.14-017 | 单元 | OscillationDetector 过度调整 |
| AC-v0.14-018 | 单元 | OscillationDetector 策略切换振荡 |
| AC-v0.14-019 | 单元 | OscillationDetector 正常历史不误报 |
| AC-v0.14-020 | 单元 | OscillationDetector 空历史 → false |
| AC-v0.14-021 | 单元 | AdjustmentHistory record + recent/since |
| AC-v0.14-022 | 单元 | AdjustmentHistory successfulAdjustmentCount |
| AC-v0.14-023 | 单元 | AdjustmentHistory 线程安全 |
| AC-v0.14-024 | 单元 | FeedbackCalibrator 权重偏离默认值 |
| AC-v0.14-025 | 单元 | FeedbackCalibrator 权重和=1.0, 范围[0.10,0.50] |
| AC-v0.14-026 | 单元 | FeedbackCalibrator 不足窗口 → 不校准 |
| AC-v0.14-027 | 单元 | LoopSession 构造验证 |
| AC-v0.14-028 | 编译 | LoopEvidenceRecorder 接口方法 |
| AC-v0.14-029 | 单元 | SafetyGate cooldown REJECTED → continue |
| AC-v0.14-030 | 单元 | 振荡 emergencyStopThreshold 次 → EMERGENCY_STOPPED |
| AC-v0.14-031 | 单元 | SafetyGate REJECTED/NO_OP 不停止循环 |
| AC-v0.14-032 | 集成 | E2E: 闭环 ≥5 周期 + ≥1 实际调整 |
| AC-v0.14-033 | 集成 | E2E: 振荡 → EMERGENCY_STOPPED |
| AC-v0.14-034 | 回归 | `mvn test` 774 + 新增，零失败 |
| AC-v0.14-035 | 单元 | reset() STOPPED/EMERGENCY_STOPPED → IDLE |

---

## 8. 范围外（再次声明）

- 多 executor 并行闭环协调（DFR-01 → v0.15.0）
- 闭环性能基准（DFR-02 → v0.15.0+）
- 策略参数自动调优/生成（DFR-03 → v0.16.0+）
- 基于比较运行的交叉验证（DFR-04 → v0.15.0）
- 多维度同时调整（DFR-06 → v0.16.0+）
- 新 executor mutation 或动态配置维度

---

## 9. SR 阶段出口条件

进入 OpenSpec change decomposition 前必须完成：

1. `21-sr-review.md` — 独立 SR review findings
2. `22-sr-review-disposition.md` — 逐项处置 findings
3. `23-sr-closure-verification.md` — 独立验证 P0/P1 已关闭
4. 更新 `docs/00-project/current-state.md`，明确是否允许进入 change decomposition
5. 不得在 SR closure 前创建 OpenSpec change、Java 实现

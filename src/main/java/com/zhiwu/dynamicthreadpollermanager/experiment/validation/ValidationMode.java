package com.zhiwu.dynamicthreadpollermanager.experiment.validation;

/** Three execution modes for closed-loop validation comparison. */
public enum ValidationMode {
    CLOSED_LOOP,
    STATIC_POLICY,
    BASELINE
}
